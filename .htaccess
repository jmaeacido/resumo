<IfModule mod_rewrite.c>
    Options +FollowSymLinks -Indexes
    RewriteEngine On
    RewriteBase /resumo/

    RewriteCond %{REQUEST_URI} !^/resumo/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
